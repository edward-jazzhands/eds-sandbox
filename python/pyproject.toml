[project]
name = "python"
version = "0.1.0"
description = "Eds Sandbox python environment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=2.1.1",
]

[dependency-groups]
dev = [
    "textual-dev>=1.7.0",
]
